# Firmware bundle signing keys

The files in this directory are **test-only** signing materials used by
`cargo xtask wallet-firmware` and CI verification jobs. They should never be
reused for production firmware signing.
