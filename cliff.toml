# git-cliff ~ configuration file
# https://git-cliff.org/docs/configuration


[changelog]
# A Tera template to be rendered as the changelog's footer.
# See https://keats.github.io/tera/docs/#introduction
header = """
# Changelog\n
All notable changes to this project will be documented in this file.\n
"""
# A Tera template to be rendered for each release in the changelog.
# See https://keats.github.io/tera/docs/#introduction
body = """
{% macro wallet_commit_line(commit) -%}
        - {% if commit.scope %}*({{ commit.scope }})* {% endif %}{% if commit.breaking %}[**breaking**] {% endif %}{{ commit.message | upper_first }}\
{%- endmacro %}

{% macro wallet_section(slug, title, commits) -%}
{% set rendered = false -%}
{% for commit in commits -%}
    {% if commit.body and (commit.body is containing(slug)) -%}
        {% if not rendered -%}
    #### {{ title }}
        {% set rendered = true -%}
        {% endif -%}
{{ wallet_commit_line(commit) }}
    {% endif -%}
{% endfor -%}
{%- endmacro %}

{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
{{ wallet_commit_line(commit) }}
    {% endfor %}
{% endfor %}\
{% set wallet_has = false -%}
{% for commit in commits -%}
    {% if commit.body and (commit.body is containing("Wallet-Labels:")) -%}
        {% set wallet_has = true -%}
    {% endif -%}
{% endfor -%}
    {% if wallet_has -%}

    ### 👛 Wallet readiness
    {% set wallet_sections = [
        {"slug": "wallet::engine", "title": "Engine & policy updates"},
        {"slug": "wallet::gui", "title": "GUI & UX"},
        {"slug": "wallet::security", "title": "Security, supply chain, and RPC"},
        {"slug": "wallet::release", "title": "Release governance & docs"}
    ] -%}
    {% for section in wallet_sections -%}
{{ wallet_section(section.slug, section.title, commits) }}
    {% endfor -%}
    {% set other_rendered = false -%}
    {% for commit in commits -%}
        {% if commit.body and (commit.body is containing("Wallet-Labels:")) -%}
            {% if commit.body is containing("wallet::engine") or commit.body is containing("wallet::gui") or commit.body is containing("wallet::security") or commit.body is containing("wallet::release") -%}
            {% else -%}
                {% if not other_rendered -%}
    #### Other wallet improvements
                {% set other_rendered = true -%}
                {% endif -%}
{{ wallet_commit_line(commit) }}
            {% endif -%}
        {% endif -%}
    {% endfor -%}
    {% endif -%}\n
    {% set vector_rendered = false -%}
    {% for commit in commits -%}
        {% if commit.message is containing("vector") or (commit.body and (commit.body is containing("vector"))) -%}
            {% if not vector_rendered -%}
    ### 🧬 ZK vectors
            {% set vector_rendered = true -%}
            {% endif -%}
{{ wallet_commit_line(commit) }}
        {% endif -%}
    {% endfor -%}
    {% if not vector_rendered -%}

    ### 🧬 ZK vectors
    _Add entries for deterministic vector refreshes when `vendor/rpp-stark/vectors/` changes._
    {% endif -%}\n
"""
# A Tera template to be rendered as the changelog's footer.
# See https://keats.github.io/tera/docs/#introduction
footer = """
<!-- generated by git-cliff -->
"""
# Remove leading and trailing whitespaces from the changelog's body.
trim = true
# Render body even when there are no releases to process.
render_always = true
# An array of regex based postprocessors to modify the changelog.
postprocessors = [
    # Replace the placeholder <REPO> with a URL.
    { pattern = '<REPO>', replace = "https://github.com/ava-labs/chain" },
]
# render body even when there are no releases to process
# render_always = true
# output file path
# output = "test.md"

[git]
# Parse commits according to the conventional commits specification.
# See https://www.conventionalcommits.org
conventional_commits = true
# Exclude commits that do not match the conventional commits specification.
filter_unconventional = true
# Require all commits to be conventional.
# Takes precedence over filter_unconventional.
require_conventional = false
# Split commits on newlines, treating each line as an individual commit.
split_commits = false
# An array of regex based parsers to modify commit messages prior to further processing.
commit_preprocessors = [
    { pattern = ".*", replace_command = "python3 scripts/git_cliff_wallet_labels.py" },
    # Replace issue numbers with link templates to be updated in `changelog.postprocessors`.
    #{ pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](<REPO>/issues/${2}))"},
    # Check spelling of the commit message using https://github.com/crate-ci/typos.
    # If the spelling is incorrect, it will be fixed automatically.
    #{ pattern = '.*', replace_command = 'typos --write-changes -' },
    # Squash merges end with the pull request number (e.g., `(#12345)`). Replace those with a link to the pull request.
    { pattern = '\(#([0-9]+)\)', replace = '([#${1}](<REPO>/pull/${1}))' },
]
# Prevent commits that are breaking from being excluded by commit parsers.
protect_breaking_commits = false
# An array of regex based parsers for extracting data from the commit message.
# Assigns commits to groups.
# Optionally sets the commit's scope and can decide to exclude commits from further processing.
commit_parsers = [
    { message = "^feat", group = "<!-- 0 -->🚀 Features" },
    { message = "^fix", group = "<!-- 1 -->🐛 Bug Fixes" },
    { message = "^doc", group = "<!-- 3 -->📚 Documentation" },
    { message = "^perf", group = "<!-- 4 -->⚡ Performance" },
    { message = "^refactor", group = "<!-- 2 -->🚜 Refactor" },
    { message = "^style", group = "<!-- 5 -->🎨 Styling" },
    { message = "^test", group = "<!-- 6 -->🧪 Testing" },
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore\\(pr\\)", skip = true },
    { message = "^chore\\(pull\\)", skip = true },
    { message = "^chore|^ci", group = "<!-- 7 -->⚙️ Miscellaneous Tasks" },
    { body = ".*security", group = "<!-- 8 -->🛡️ Security" },
    { message = "^revert", group = "<!-- 9 -->◀️ Revert" },
    { message = ".*", group = "<!-- 10 -->💼 Other" },
]
# Exclude commits that are not matched by any commit parser.
filter_commits = false
# An array of link parsers for extracting external references, and turning them into URLs, using regex.
link_parsers = []
# Include only the tags that belong to the current branch.
use_branch_tags = true
# Order releases topologically instead of chronologically.
topo_order = false
# Order releases topologically instead of chronologically.
topo_order_commits = true
# Order of commits in each group/release within the changelog.
# Allowed values: newest, oldest
sort_commits = "oldest"
# Process submodules commits
recurse_submodules = false
# Only process tags in this pattern
tag_pattern = "v[0-9].*"
