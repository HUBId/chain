name: Stable Nightly Scan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run stable migration scan (warn mode)
        run: scripts/ci/stable_scan
