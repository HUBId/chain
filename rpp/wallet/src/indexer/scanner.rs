//! Placeholder for future indexer scanning utilities.

/// Empty scanner stub so the module tree compiles.
pub struct Scanner;

impl Scanner {
    #[allow(dead_code)]
    pub fn new() -> Self {
        Self
    }
}
